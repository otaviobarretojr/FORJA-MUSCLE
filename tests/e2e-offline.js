const { chromium } = require('playwright');
const assert = require('node:assert/strict');
const BASE='http://127.0.0.1:4173/';
const CACHE='vitafit-v4-core';
(async()=>{const browser=await chromium.launch({headless:true});try{const context=await browser.newContext({viewport:{width:412,height:915},hasTouch:true,isMobile:true});const page=await context.newPage();const errors=[];page.on('pageerror',e=>errors.push(String(e)));await page.addInitScript(()=>localStorage.setItem('shape12.e2e.offlinePersist',JSON.stringify('preservado')));await page.goto(`${BASE}?e2e=offline-v4`,{waitUntil:'domcontentloaded'});await page.waitForFunction(()=>document.documentElement.dataset.vitafitReady==='true');await page.evaluate(()=>navigator.serviceWorker.ready);await page.waitForFunction(()=>navigator.serviceWorker.controller?.state==='activated',null,{timeout:15000});const prepared=await page.evaluate(async cacheName=>{const reg=await navigator.serviceWorker.ready,cache=await caches.open(cacheName),requests=await cache.keys();return{active:!!reg.active,controller:!!navigator.serviceWorker.controller,cacheNames:await caches.keys(),urls:requests.map(r=>r.url),build:document.documentElement.dataset.vitafitBuild,fast:window.setAppScreen?.__vitafitFast===true}},CACHE);assert.equal(prepared.active,true);assert.equal(prepared.controller,true);assert.equal(prepared.build,'4.0.0');assert.equal(prepared.fast,true);assert.ok(prepared.cacheNames.includes(CACHE));for(const suffix of ['/index.html','/css/v4.css','/js/v4.js','/manifest.webmanifest','/icons/vitafit.svg'])assert.ok(prepared.urls.some(url=>new URL(url).pathname.endsWith(suffix)),`não pré-cacheado: ${suffix}`);assert.ok(!prepared.urls.some(url=>/nutrition|fragments\/|v3[0-9]|v36[0-9]/i.test(url)),'cache contém runtime legado');await context.setOffline(true);await page.reload({waitUntil:'domcontentloaded',timeout:20000});await page.waitForFunction(()=>document.documentElement.dataset.vitafitReady==='true',null,{timeout:20000});const state=await page.evaluate(()=>({online:navigator.onLine,controller:!!navigator.serviceWorker.controller,build:document.documentElement.dataset.vitafitBuild,nav:document.querySelectorAll('.nav-btn').length,persisted:localStorage.getItem('shape12.e2e.offlinePersist')}));assert.equal(state.online,false);assert.equal(state.controller,true);assert.equal(state.build,'4.0.0');assert.equal(state.nav,3);assert.equal(JSON.parse(state.persisted),'preservado');for(const id of ['treino','mais','hoje']){await page.evaluate(id=>window.setAppScreen(id),id);const active=await page.evaluate(id=>document.getElementById(id==='hoje'?'screen-hoje':id)?.classList.contains('active'),id);assert.equal(active,true)}const resources=await page.evaluate(async()=>{const out=[];for(const url of ['css/v4.css?v=4.0.0','js/v4.js?v=4.0.0']){try{const r=await fetch(url);out.push({url,ok:r.ok,status:r.status})}catch(e){out.push({url,ok:false,status:0})}}return out});for(const r of resources)assert.equal(r.ok,true,JSON.stringify(r));assert.deepEqual(errors,[],`Erros JS: ${errors.join(' | ')}`);console.log('VITAFIT V4 offline audit: OK')}finally{await browser.close()}})().catch(err=>{console.error(err);process.exit(1)});